#!/usr/bin/env bash
# implement.md §5 – run bench/runBench.ts for the pinned sweep and write
# bench/results.json. Commit results.json so the demo renders without testnet.
set -euo pipefail
echo "scripts/bench.sh: not yet implemented (lands with the prover CLI)"
exit 1
