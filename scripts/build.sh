#!/usr/bin/env bash
# implement.md §7 – build circuits + contracts.
# Runs nargo build for circuits/, cargo build --release --target wasm32-unknown-unknown
# for contracts/, snarkjs setup for circuits/groth16_batch/. Filled in per tier.
set -euo pipefail
echo "scripts/build.sh: not yet implemented (Tier 0 setup pending)"
exit 1
