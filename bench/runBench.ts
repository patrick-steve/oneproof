// implement.md §5 – run {naive, batch, recursive} x N over the pinned sweep,
// write bench/results.json. Numbers MUST be real measurements from testnet;
// omit a run if it didn't execute (never zeros).

export {};
