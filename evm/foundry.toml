[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
optimizer = true
optimizer_runs = 200
solc = "0.8.24"

# Gas reports for the verify test so the EVM-side number is captured
gas_reports = ["Verifier", "Groth16Verifier"]
