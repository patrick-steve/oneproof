// implement.md §4.1 – inner private-transfer circuit (UltraHonk leaf).
// Statement: Poseidon commitment + Merkle membership (depth 16) + nullifier.
// Public: root, nullifier. Private: secret, amount, blinding, Merkle path.
// Implemented in Tier 2 step 4.1.

fn main() {}
