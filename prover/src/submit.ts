// implement.md §4.4 – build Soroban tx, simulate to read resource cost,
// send, return { txHash, cpuInstructions, resourceFeeStroops, success }.
// MUST read real measured resource numbers from the network; never estimate.

export {};
